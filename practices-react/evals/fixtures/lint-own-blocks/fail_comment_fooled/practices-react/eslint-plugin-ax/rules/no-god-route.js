// Fixture stub for lint_own_blocks_guard.sh (P3-81).
// The guard reads only this file's BASENAME to derive the catalog rule list;
// no rule implementation is needed here.
module.exports = { meta: { type: "problem" }, create: () => ({}) }
