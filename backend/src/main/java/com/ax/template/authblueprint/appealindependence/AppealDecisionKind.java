package com.ax.template.authblueprint.appealindependence;

/** ORIGINAL = level 0 (the decision being appealed); APPEAL = level >= 1 (an appeal in the chain). */
public enum AppealDecisionKind {
    ORIGINAL,
    APPEAL
}
