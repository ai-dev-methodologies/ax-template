package com.ax.template.authblueprint.additivefacts;

/** FACT-CLOSED-PERIOD-ADD-003 — one-way; a CLOSED period's frozen aggregate never rewrites. */
public enum FactPeriodStatus {
    OPEN,
    CLOSED
}
