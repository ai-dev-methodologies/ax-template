package com.ax.template.authblueprint.quorumresolution;

/** The three mutually-exclusive ballot choices. ABSTAIN is a CAST act (counts toward quorum participation). */
public enum Choice {
    YES,
    NO,
    ABSTAIN
}
