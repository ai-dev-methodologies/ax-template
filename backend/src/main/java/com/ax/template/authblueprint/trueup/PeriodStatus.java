package com.ax.template.authblueprint.trueup;

/** TUP-SEALED-001 — one-way lifecycle: OPEN → CLOSED (run-of-record fixed) → SEALED (fail-closed). */
public enum PeriodStatus { OPEN, CLOSED, SEALED }
