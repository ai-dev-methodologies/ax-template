package ddd.fixtures.widget;
/** FIXTURE — deliberately a MEMBER repository via a custom base + line-broken type param (AM1). */
public interface WidgetPartRepo extends BaseRepo<WidgetPart,
        Long> {}
