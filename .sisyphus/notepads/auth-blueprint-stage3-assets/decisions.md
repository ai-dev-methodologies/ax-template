
### Task 7: Quality Companion Baseline
- Decided to embed a strict `zero-warning` goal and `verify/manifest.schema.json` placeholder into the Quality Gates section to prevent technical debt accumulation and reinforce the verify triplet.
- Task 8: Decided to patch the evidence map to explicitly map Stage 3 items to Stage 4 gates, preventing the false claim that curated promotion is already achieved just because drafts exist.

### Final Verification Wave F2 Fix
- Decided to add a compact `Auth State Flow` section to the architecture baseline instead of rewriting the whole document, so the draft stays policy-level but becomes operational.
- Decided to make `.sisyphus/drafts/auth-blueprint-curated-evidence-map.md` the explicit downstream handoff target in the quality companion.
