# legacy-src — upstream snapshot (pre-receipts era)

A snapshot whose ORIGINAL fetched bytes are gone. Its manifest record cannot be corrected by
editing a number without manufacturing a provenance claim about bytes nobody can attest to.
