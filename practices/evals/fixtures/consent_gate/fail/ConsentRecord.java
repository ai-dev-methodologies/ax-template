package com.example.share;

import jakarta.persistence.Entity;

/**
 * Adoption marker: this tree has adopted the consent ledger, so the guard scans
 * its data-sharing methods. (Minimal stub — real shape is common/ConsentRecord.)
 */
@Entity
public class ConsentRecord {
}
