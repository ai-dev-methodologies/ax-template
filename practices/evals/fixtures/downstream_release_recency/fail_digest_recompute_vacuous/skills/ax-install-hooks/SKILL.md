# ax-install-hooks (fixture)

This SKILL.md declares NO `ax:artifact` marker, so marker discovery across
`skills/*/SKILL.md` yields the empty map — the same state a tree with no
`skills/` directory, an ls-tree that returned nothing, or a marker syntax the
parser stopped recognising would produce.
