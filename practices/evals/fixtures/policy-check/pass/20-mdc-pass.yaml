# Policy-check eval fixture
# Query mdc tag matches MDC trace propagation rules
description: "Query mdc tag matches MDC trace propagation rules"
query:
  domain: mdc
expect:
  contains:
    - PRACTICES-OBS-002
  not_contains:
    - PRACTICES-PERS-005
    - PRACTICES-ERR-001
