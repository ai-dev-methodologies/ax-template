# Policy-check eval fixture
# Query validation tag matches validation rules
description: "Query validation tag matches validation rules"
query:
  domain: validation
expect:
  contains:
    - PRACTICES-VAL-001
  not_contains:
    - PRACTICES-PERS-005
    - PRACTICES-CACHE-001
