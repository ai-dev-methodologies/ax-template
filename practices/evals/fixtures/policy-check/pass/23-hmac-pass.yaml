# Policy-check eval fixture
# Query hmac tag matches webhook HMAC rules
description: "Query hmac tag matches webhook HMAC rules"
query:
  domain: hmac
expect:
  contains:
    - PRACTICES-INTEG-001
  not_contains:
    - PRACTICES-PERS-005
    - PRACTICES-ERR-001
