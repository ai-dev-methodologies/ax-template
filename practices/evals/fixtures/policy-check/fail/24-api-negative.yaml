# Policy-check eval fixture
# API tag does not return transaction or soft-delete rules
description: "API tag does not return transaction or soft-delete rules"
query:
  domain: api
expect:
  contains:
  not_contains:
    - PRACTICES-TX-001
    - PRACTICES-PERS-005
