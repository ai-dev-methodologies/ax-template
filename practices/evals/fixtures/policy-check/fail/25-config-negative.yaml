# Policy-check eval fixture
# Config tag does not return persistence or error rules
description: "Config tag does not return persistence or error rules"
query:
  domain: config
expect:
  contains:
  not_contains:
    - PRACTICES-PERS-005
    - PRACTICES-ERR-001
