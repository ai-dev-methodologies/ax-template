# Policy-check eval fixture
# Web tag does not return transaction rules
description: "Web tag does not return transaction rules"
query:
  domain: web
expect:
  contains:
  not_contains:
    - PRACTICES-TX-001
    - PRACTICES-PERS-005
