# Policy-check eval fixture
# Cache tag does not return soft-delete or error rules
description: "Cache tag does not return soft-delete or error rules"
query:
  domain: cache
expect:
  contains:
  not_contains:
    - PRACTICES-PERS-005
    - PRACTICES-ERR-001
