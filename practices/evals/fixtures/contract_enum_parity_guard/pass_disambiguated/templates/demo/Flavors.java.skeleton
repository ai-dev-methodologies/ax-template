package com.demo.template;

/**
 * wire_source: java_enum_decl — a copy-target skeleton declares the vocabulary the
 * reference workload itself does not implement (the billing InvoiceStatus shape).
 */
public class Flavors {

    public enum Flavor { MILD, HOT }
}
