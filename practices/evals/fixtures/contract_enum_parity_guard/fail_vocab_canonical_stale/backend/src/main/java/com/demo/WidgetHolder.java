package com.demo;

/** Nested-in-entity shape (mirrors WebhookDelivery.java.skeleton:38). */
public class WidgetHolder {

    public enum Kind { ALPHA, BETA }

    private Kind kind;

    public Kind getKind() { return kind; }
}
