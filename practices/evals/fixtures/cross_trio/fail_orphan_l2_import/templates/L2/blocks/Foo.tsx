// Foo.tsx — L2 block WITHOUT evidence frontmatter (intentional — this is the failing case)
import React from 'react';

export function Foo() {
  return <div>Foo component without evidence anchoring</div>;
}
