# trio_integrity/fail_missing_frontend_yaml — expected exit 1

auth backend spec exists with frontend_required: true but specs/auth-frontend-l0.yaml is absent.

Expected: exit 1, stderr contains `MISSING_FRONTEND_SPEC`
