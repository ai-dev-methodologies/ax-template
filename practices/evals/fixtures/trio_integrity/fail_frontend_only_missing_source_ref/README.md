# trio_integrity/fail_frontend_only_missing_source_ref — expected exit 1

frontend_only route has backend_operation_id: null but static_source_ref is empty/missing.

Expected: exit 1, stderr contains `frontend_only route missing static_source_ref`
