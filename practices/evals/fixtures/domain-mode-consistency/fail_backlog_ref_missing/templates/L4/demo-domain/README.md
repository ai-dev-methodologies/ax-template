# demo-domain (L4 fixture)

**Status**: backend-only (fixture)

Narrative below the Status value is deliberately present: it names the OTHER mode the way
real L4 READMEs do ("shipped as a backend-only stub, later promoted"), which the guard must
NOT read as a second declaration.
