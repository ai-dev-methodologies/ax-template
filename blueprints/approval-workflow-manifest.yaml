template_id: "approval-workflow-blueprint"
version: "0.1.0"

when_to_use:
  - "Multi-step approval chains for internal business actions — budget requests, document publishing, vendor onboarding, access provisioning"
  - "Korean enterprise 결재 surfaces — requester → 1차 결재 → 2차 결재 → ... → 최종 결재"
  - "Ordered approval where step N+1 should not be reachable until step N is APPROVED (sequential, not parallel)"

not_for:
  - "Parallel approval (any-of-N) — extend the state machine before adopting"
  - "Dynamic approver routing based on request content (manager-of-requester lookup, etc.) — fork-receiver decides the resolver but this blueprint takes static approver list"
  - "Long-running multi-day workflows requiring reminders / escalation — escalation is out of scope for v1"
  - "Public-facing approval (customer review submissions) — this is an INTERNAL operations blueprint"
  # ── R31-iter2 dogfood closures (explicit scope guards) ───────────────────
  - "대결 / 위임 (delegate, reassign mid-flight) — a step's approverUserId is updatable=false at the JPA layer. Fork-receivers needing delegation should add a DelegationLink side table that maps original-approver → delegate-approver at the filter boundary, not by mutating this blueprint."
  - "Multi-approver per step (요구사항: 한 단계에 N명 모두 승인) — one approverUserId per step row. Use multiple sequential steps for N-of-N 결재 (N개 row), or fork the state machine to PENDING_PARTIAL → APPROVED for true any-of-N / all-of-N semantics."
  - "Event publication to external systems on transition — completion / rejection events do NOT auto-publish to notification / audit-log / webhook domains. Fork-receivers compose those domains via a domain event listener at the controller boundary."
  - "Multi-tenant tenant scoping — v1 assumes single-tenant. Fork-receivers adopting multi-tenant SaaS MUST add a tenant_id column to approval_requests + approval_steps, an AOP guard against cross-tenant query leakage, and update SecurityConfig to derive the tenant from the authenticated principal. See specs/multi-tenant-l0.yaml#ISOLATION-001/002/003."

# ─── deferred to v2 (R31-iter2 dogfood explicit deferrals) ─────────────────
deferred_to_v2:
  - id: "list_pagination"
    reason: "GET /api/approvals + /inbox return the full set. Acceptable for v1 (request_limit_per_user soft cap = 200). v2 should add page/size + Cursor for tenants with >200 requests."
  - id: "list_status_filter"
    reason: "GET /api/approvals returns all of caller's own requests regardless of status. v2 should add ?status=APPROVED,REJECTED,SUBMITTED query parameter so 'show my completed approvals' is a single call."
  - id: "explicit_cancel_actor_column"
    reason: "ApprovalRequest currently relies on the invariant 'only requester can cancel' to infer the cancel actor. v2 should add cancelled_by_user_id + cancel_reason for true delegated cancel (admin override) scenarios."

# ─── lifecycle ─────────────────────────────────────────────────────────────
state_machines:
  request:
    description: "Sole mutator: ApprovalRequestStateMachine"
    transitions:
      - from: DRAFT
        to: [SUBMITTED, CANCELLED]
      - from: SUBMITTED
        to: [APPROVED, REJECTED, CANCELLED]
      - from: APPROVED
        to: []
      - from: REJECTED
        to: []
      - from: CANCELLED
        to: []
  step:
    description: "Sole mutator: ApprovalStepStateMachine"
    transitions:
      - from: PENDING
        to: [APPROVED, REJECTED]
      - from: APPROVED
        to: []
      - from: REJECTED
        to: []

lifecycle:
  cascade_on_step_approval: "When the last step transitions PENDING→APPROVED, the request transitions SUBMITTED→APPROVED in the SAME transaction. Partial state never observable."
  cascade_on_step_rejection: "Single step REJECTED → request immediately REJECTED. Remaining PENDING steps are no longer actionable (request is terminal)."
  cancellation_window: "DRAFT or SUBMITTED — requester only. Terminal states are not cancellable."

# ─── step ordering ─────────────────────────────────────────────────────────
step_ordering:
  rule: "Steps act in strict ascending orderIndex. Acting on step N requires every step with orderIndex < N to be APPROVED."
  enforcement: "ApprovalService.approve() / reject() loads all sibling steps; out-of-order action returns 409 STEP_OUT_OF_ORDER."

# ─── step validation (R31-iter1 dogfood closure) ───────────────────────────
step_validation:
  duplicate_approvers_rejected: true
  duplicate_approver_response:
    status: 400
    error_code: DUPLICATE_APPROVER
  self_approve_rejected: true
  self_approve_response:
    status: 400
    error_code: SELF_APPROVE_FORBIDDEN
  self_approve_policy_note: >
    Korean enterprise 결재 default — 본인 결재 금지. Fork-receivers needing self-approve
    (e.g. test fixtures, single-person small-team workflows) override via
    {@code approval-workflow.allow-self-approve=true} in application.yml.

# ─── payload immutability (R31-iter1 dogfood closure) ──────────────────────
payload_immutability:
  rule: "payload_json column is @Column(updatable=false). Hibernate ignores any update attempt."
  motivation: >
    P1 (KG이니시스 fintech) dogfood — a writer-side path that mutated payload mid-flight
    would let a requester swap the substance under approvers between their reviews
    (tampering risk on insurance claims, expense reports, vendor contracts).

# ─── step audit ────────────────────────────────────────────────────────────
step_audit:
  capture:
    - actedByUserId
    - actedAt
    - comment (optional, <= 1024 chars)
  immutability: "Once a step's status is APPROVED or REJECTED, actedBy / actedAt / comment cannot be mutated. The step entity's setters for these fields are package-private and called only by the state machine."

# ─── authz ─────────────────────────────────────────────────────────────────
authz:
  endpoint_pattern: "/api/approvals/**"
  authentication_required: true
  cross_user_response: 404_not_403
  step_action_response_on_wrong_actor: 403
  visibility_rule: >
    A request is visible to (a) its requester always, (b) its approvers while the request
    is in status SUBMITTED, and (c) — P3-63, narrow — an approver who ACTED
    (step.actedByUserId = caller), once the request has reached a terminal state, so they
    can see the outcome of their own decision. DRAFT requests stay hidden from approvers
    so work-in-progress isn't leaked, and an assigned-but-UNACTED approver gets nothing
    after the request terminates: an upstream rejection must not retroactively disclose the
    request to approvers who were never reached.
  shared_predicates: >
    P2-38a — the rules above are NOT restated per call site. ApprovalActionGuards
    (isActionable / isAssignedApprover / isNextActionableStep / isRequester / canView /
    hasActed) is the single implementation; ApprovalService's action path, the single-GET
    read path and ApprovalActionEvaluator all call it. The previous JPQL copy of the
    visibility rule (ApprovalRequestRepository.findVisibleTo) has been DELETED — it served
    exactly one caller and no other surface could consult it.
  advertised_action_set: >
    P2-38b — ApprovalRequestResponse.allowedActions carries the caller's permitted actions
    on the wire, computed by ApprovalActionEvaluator (guards + the two state machines'
    transition tables, zero local policy). Fork-receiver clients SHOULD consume it rather
    than re-derive; templates/L0/fork-receiver-kit/authorized-actions.ts does exactly that
    and is the single client-side selector (P2-39), imported by both the L4 detail page and
    the cross-boundary parity test.

# ─── query ─────────────────────────────────────────────────────────────────
query:
  endpoints:
    list_my_requests: "GET /api/approvals — returns rows where requesterUserId = caller, newest first"
    inbox: "GET /api/approvals/inbox — returns PENDING steps where step.approverUserId = caller AND request.status = SUBMITTED, oldest request first"
  pagination: "v1 returns the full set (defaults bounded by request_limit_per_user). v2 should add page/size."

# ─── limits ────────────────────────────────────────────────────────────────
limits:
  max_steps_per_request: 10
  max_payload_bytes: 16384
  request_limit_per_user: 200    # soft cap; not enforced in v1, surfaced for fork-receivers

testing_baseline:
  unit: "JUnit 5 + AssertJ — both state machines"
  integration: "RestAssured black-box HTTP against @SpringBootTest RANDOM_PORT"

verification_checkpoints:
  - "DRAFT creation is invisible to approvers (WF-LIFECYCLE-001)"
  - "Last step approve cascades request to APPROVED atomically (WF-LIFECYCLE-002)"
  - "Single step reject cascades request to REJECTED immediately (WF-LIFECYCLE-003)"
  - "Illegal state transitions throw (WF-LIFECYCLE-004)"
  - "Cross-user IDOR returns 404 (WF-AUTHZ-002)"
  - "Wrong approver returns 403 (WF-AUTHZ-003)"
  - "An approver who ACTED can still GET the request after it goes terminal; a DRAFT assignee, an unacted later approver after a rejection, and an unrelated user all get 404 (WF-AUTHZ-002, P3-63)"
  - "allowedActions on the live response tracks caller and lifecycle stage; neutering any ApprovalActionGuards predicate turns BOTH the action-path test and the parity test red (P2-38a/b)"
  - "Out-of-order step action returns 409 (WF-STEP-001)"
  - "Comment + actor + timestamp persisted atomically with status (WF-STEP-002, WF-STEP-003)"
  - "Inbox returns oldest-first pending steps (WF-QUERY-001)"
  - "List returns only the caller's own requests (WF-QUERY-002)"

must_not:
  - "Mutate ApprovalRequest.status outside ApprovalRequestStateMachine"
  - "Mutate ApprovalStep.status / actedByUserId / actedAt outside ApprovalStepStateMachine"
  - "Allow an approver to act out of order"
  - "Allow a step action without recording the actor"
  - "Return 403 for cross-user request access (must be 404 — IDOR enumeration defense)"
  - "Allow request transitions out of terminal states"

reject_if:
  - "POST /api/approvals with > max_steps_per_request → 400 TOO_MANY_STEPS"
  - "POST /api/approvals with empty approvers list → 400 NO_APPROVERS"
  - "step action on a terminal request → 409 REQUEST_TERMINAL"
  - "step action by non-approver → 403 NOT_APPROVER"
  - "step action out of order → 409 STEP_OUT_OF_ORDER"

source_precedence:
  - "ITIL v4 change-management approval workflow (multi-step, ordered)"
  - "Korean enterprise 결재 workflow practice (sequential approval, 결재선)"
  - "OWASP ASVS V4 — authorization (visibility per role, IDOR-safe responses)"

# ─── routing — attribute-resolved approval routing (WF-ROUTE-001/002) ─────
routing:
  description: >
    An OPT-IN alternative to the direct approverUserIds path. A request may instead
    carry (category, amount); ApprovalService.submit() resolves the ordered
    approver-role chain from the matching RoutingRule EXACTLY ONCE, at submission
    (not at creation) — the resolved chain is snapshotted onto the request
    (resolvedChainJson) and the request's steps are built from it.
  mutual_exclusivity: "approverUserIds (direct mode) and category+amount (routing mode) are mutually exclusive; a request with neither is rejected 400 ROUTING_ATTRIBUTES_REQUIRED."
  rule_matching: "RoutingRule bands are half-open [minAmount, maxAmount) per (categoryOrDept); the first rule matching, ordered by minAmount, is used."
  immutability: "Once resolved, resolvedChainJson is written by a sole-mutator setter that rejects a second write. A routing rule change or deletion AFTER a request has resolved never rewrites that request — RoutingRule is a standalone config entity, never re-joined against after submission."
  fail_closed: "No matching RoutingRule for the request's (category, amount) → 422 NO_MATCHING_ROUTING_RULE at submit(); the request stays DRAFT (no silent default chain)."
  rule_management: "GET /api/approvals/routing-rules is any authenticated caller (queryable); POST/DELETE are ROLE_ADMIN-only."

# Multi-tenant deferral note (R37 guard requirement):
#   Single-tenant default per specs/multi-tenant-l0.yaml#MULTI-TENANT-ISOLATION-DEFAULT-001
