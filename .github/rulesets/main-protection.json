{
  "_comment": "Source-of-truth for branch protection on main. Applied via practices/scripts/setup-branch-protection.sh. Drift between this file and the live GitHub state is the policy violation, not the inverse. The schema below targets the GitHub REST API endpoint PUT /repos/{owner}/{repo}/branches/main/protection.",
  "required_status_checks": {
    "strict": true,
    "contexts": [
      "practices-sentinel / guards"
    ]
  },
  "enforce_admins": true,
  "required_pull_request_reviews": {
    "required_approving_review_count": 0,
    "dismiss_stale_reviews": false,
    "require_code_owner_reviews": false,
    "require_last_push_approval": false
  },
  "restrictions": null,
  "required_linear_history": true,
  "allow_force_pushes": false,
  "allow_deletions": false,
  "required_conversation_resolution": true,
  "lock_branch": false,
  "allow_fork_syncing": false
}
