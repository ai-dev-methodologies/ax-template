# engine — consumer-proof harness wave-2 iter2 dogfood
# Persona: engine (automated harness discovering enforcement gaps)
#
# Scope: cell S2.MONEY-QUANTITY.XB — cross-boundary money minor-unit
# contract parity (CANARY-008). Single-writer INTEGRATION lane: this file
# records the closure that landed this wave, not a fresh discovery pass.
iteration: 2
closed_cell: S2.MONEY-QUANTITY.XB
findings:
  - persona: engine-w2
    finding: "F1 (CLOSED, methodology_gap): no test validated FE money.ts minor-unit parsing against the actual BE-emitted payment JSON contract in an integrated run (CANARY-008 — verified absent at plant time 2026-07-20). Closed this wave: backend/src/test/java/com/ax/template/authblueprint/payment/MoneyContractParityTest.java (BE, @Tag(\"PAYMENT\") -> ./gradlew testPayment) and frontend/tests/money-contract-parity.vitest.ts (FE) both read the SAME committed golden fixture (frontend/tests/_fixtures/money-contract.golden.json), built exactly the way PaymentService/RefundService emit PaymentResponse/RefundResponse. Mutation-lock evidence (by construction, not yet gradle-verified this session per the INTEGRATION-lane no-gradle rule): a drift in either side's field shape trips exactly one of the two tests. Closes CANARY-008 (see canary-gaps.yaml staleness_note) and flips coverage-map.yaml S2.MONEY-QUANTITY.XB partial->covered."
    classification: methodology_gap
    references_artifact_path: "backend/src/test/java/com/ax/template/authblueprint/payment/MoneyContractParityTest.java, frontend/tests/money-contract-parity.vitest.ts, frontend/tests/_fixtures/money-contract.golden.json"
  - persona: engine-w2
    finding: "F2 (OPEN, scope_deferral — genuinely live real bug, NOT closed this wave): the parity test's own golden fixture surfaced a real production wire-contract mismatch. PaymentResponse.amount/capturedAmount/balance are MAJOR-unit BigDecimal (per common.Money's own documented 'Payment / PG-edge layer' convention), scaled to the currency's ISO-4217 minor-unit count by PaymentService#scale — this contradicts BOTH (a) contracts/payment-openapi.yaml's MoneyAmount schema (integer-minor OR decimal-string; rejects a bare float with 400) and (b) templates/L0/fork-receiver-kit/money.ts's parseMinor (assumes minor units). A fork-receiver calling parseMinor on a USD PaymentResponse either throws or misreads the amount 100x. This is exactly the class of defect the parity test exists to catch — it is now a documented, falsifiable fact instead of a silent assumption, but the mismatch itself is UNFIXED this wave. Registered docs/BACKLOG.md P1-68 — this IS a genuine live P1 bug, tracked (not silently downgraded), fix scheduled for wave-3. Expiry trigger: MUST close in wave-3 (payment wire decimal-string serialization) before further money/S3 stamping — reopens/blocks wave-3."
    classification: scope_deferral
    references_artifact_path: "backend/src/test/java/com/ax/template/authblueprint/payment/MoneyContractParityTest.java, contracts/payment-openapi.yaml, templates/L0/fork-receiver-kit/money.ts, docs/BACKLOG.md"
  - persona: engine-w2
    finding: "F3 (OPEN, scope_deferral — codex cross-family review follow-up, 2026-07-20): F1's covered flip was itself VACUOUS — MoneyContractParityTest was bound to the DEAD PaymentResponse class (emitted by no endpoint); the real wire emitter is PaymentBodyMapper.toBody (extracted from PaymentController, RED-on-revert proven on amount-key rename + canonicalize neuter). Two consequences: (a) coverage-map.yaml S2.MONEY-QUANTITY.XB reverted covered->partial — the cross-boundary test is now genuine but the money contract still carries F2's defect (P1-68) UNCLOSED; (b) repointing the parity test surfaced a second, request-side defect: a contract-declared integer-minor JSON amount is deserialized as MAJOR units by MoneyDeserializer with no /100 — a contract-compliant USD $10.99 minor-unit request is silently charged $1099.00 (100x). Registered docs/BACKLOG.md P1-69 (request-side twin of P1-68), pinned by @Disabled PaymentAmountMinorUnitCharacterizationTest.java. Expiry trigger: MUST close in wave-3 alongside P1-68 (payment wire + openapi + money.ts unified to one canonical unit convention) — reopens/blocks wave-3 money reconciliation."
    classification: scope_deferral
    references_artifact_path: "practices/consumer-proof/engine/coverage-map.yaml, backend/src/main/java/com/ax/template/authblueprint/payment/PaymentBodyMapper.java, backend/src/test/java/com/ax/template/authblueprint/payment/PaymentAmountMinorUnitCharacterizationTest.java, docs/BACKLOG.md"
