# engine — consumer-proof harness wave-2 iter4 dogfood
# Persona: engine (automated harness discovering enforcement gaps)
#
# Scope: cell S2.AUTHZ.FE — mechanized caller-identity/impersonation-source
# enforcement on the FE (CANARY-010). Single-writer INTEGRATION lane: this
# file records the closure that landed this wave, not a fresh discovery pass.
iteration: 4
closed_cell: S2.AUTHZ.FE
findings:
  - persona: engine-w2
    finding: "F1 (CLOSED, methodology_gap): no ax/* ESLint rule mechanized caller-identity/impersonation enforcement on the FE — authz-relevant identity must come from the session/caller-id hook (use-caller-id.ts), not props/params, but only human-review-tier rule docs (audit-log-frontend-viewer-rbac-virtualized.md, impersonation-banner-required-when-acting-as-other-user.md, no-impersonation-bypass-via-helper-rename.md) covered the shape (CANARY-010 — verified absent at plant time 2026-07-20). Closed this wave: practices-react/eslint-plugin-ax/rules/no-caller-identity-from-props.js — the 15th ax/* rule, wired into frontend/eslint.config.mjs sharedRules AND practices-react/eslint-plugin-ax/index.js configs.recommended, shipped at error directly (a standalone Linter-API sweep across all 6 reference apps + src + packages + templates/L1 + templates/L4 = 572 files found 0 real violations pre-ship). Mutation-lock evidence: practices-react/eslint-plugin-ax/tests/no-caller-identity-from-props.test.js is a RuleTester suite (verified this session: `npm test` in practices-react/eslint-plugin-ax — 15/15 suites pass including this new one) that fails on the impersonation-prop shape and passes on the use-caller-id.ts-sourced shape; frontend/tests/use-caller-id.vitest.ts (verified this session: passes, part of the frontend `npm run test -- --run` 395/395 green) covers the hook itself. Closes CANARY-010 (see canary-gaps.yaml staleness_note) and flips coverage-map.yaml S2.AUTHZ.FE partial->covered."
    classification: methodology_gap
    references_artifact_path: "practices-react/eslint-plugin-ax/rules/no-caller-identity-from-props.js, practices-react/eslint-plugin-ax/tests/no-caller-identity-from-props.test.js, frontend/tests/use-caller-id.vitest.ts, frontend/eslint.config.mjs, practices-react/eslint-plugin-ax/index.js"
  - persona: engine-w2
    finding: "F2 (scope_deferral, codex round-5 follow-up, 2026-07-20): the F1-shipped rule flags `templates/L4/audit-log/app/(audit-log)/page.tsx` lines 184/186 — `searchParams.get('actorId')` feeding `filters` into `useQuery`/`fetchAuditLogs`. This is a LEGITIMATE admin audit-log search FILTER (filter results BY actor, a role-gated admin capability authorized server-side — the authoritative BFLA control), not the caller's own authz identity; the rule's key-name heuristic (`actorId` in IDENTITY_NAMES) cannot statically distinguish an identity-NAMED filter facet from a real caller-identity leak. Same review pass also found the `frontend/eslint.own-blocks.config.mjs` ruleset that `lint_own_blocks_guard.sh` runs against templates/L4 never wired in this rule (13 of 15 ax/* rules are listed; only `no-app-local-ui-primitives`, correctly app-scoped, and this rule are missing) — so the guard's templates/L4 sweep for this specific rule has been silently VACUOUS since the rule shipped, independent of the F1 pre-ship 572-file sweep (a one-time Linter-API check, not a standing gate). Root cause is narrower than a bare 'flat-config skips absolute-path files' claim: the copy-into-frontend/-then-lint mechanism works fine; the gap is purely an incomplete rule list in that one config, confirmed by diffing it against the 15-file practices-react/eslint-plugin-ax/rules/ disk truth. Expiry trigger: re-open when either a filter-vs-identity naming convention is adopted (so the rule can allowlist admin-filter call sites) or a second identity-NAMED filter site needs a disable and per-site disables no longer scale. Interim closure this session: justified `eslint-disable-next-line ax/no-caller-identity-from-props` added at both flagged lines in the audit-log template (host logic unchanged, `--report-unused-disable-directives` confirms both directives are load-bearing); heuristic-limitation class filed as docs/BACKLOG.md P3-55 (does not flip S2.AUTHZ.FE off `covered` — the rule itself functions correctly as defense-in-depth per its documented soundness boundary; this is a documented false-positive site, not a missed true positive)."
    classification: scope_deferral
    references_artifact_path: "templates/L4/audit-log/app/(audit-log)/page.tsx, practices-react/eslint-plugin-ax/rules/no-caller-identity-from-props.js, frontend/eslint.own-blocks.config.mjs, practices-react/evals/lint_own_blocks_guard.sh, docs/BACKLOG.md#P3-55"
